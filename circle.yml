machine:
  python:
    version: 2.7.5
test:
  override:
    - echo $PYTHONPATH; python test_project/manage.py test
dependencies:
  override:
    - pip install -r test_project/requirements/test.txt; pip install coveralls
